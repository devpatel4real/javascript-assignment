let text = "JavaScript is a powerful programming language for Web";
let word = "Script";
let result = text.includes(word);

console.log(result ? `The word "${word}" was found in text` : `The word "${word}" was not found in the text.`);

